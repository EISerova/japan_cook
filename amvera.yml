meta:
  environment: python
  toolchain:
    name: pip
    version: latest
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  scriptName: null
  command: gunicorn japan_cook.wsgi:application --bind 0.0.0.0:8000
  persistenceMount: /data
  containerPort: 8000
  servicePort: "80"
